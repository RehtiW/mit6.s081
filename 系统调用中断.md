### **1. 用户空间触发系统调用（ecall指令）**

- **关键操作**：  
    用户程序通过`ecall`指令触发系统调用（如`write`）。
    
    - **寄存器变化**：
        
        - `a7`：存放系统调用号（如SYS_write）。
            
        - `sepc`：自动保存`ecall`指令的地址。
	            
        - `scause`：设置为`8`（表示系统调用）。
            
        - `sstatus.SPP`：切换为`0`（标记中断前为U模式）。
            
        - `stvec`：指向`uservec`（用户中断处理入口）。
            

---

### **2. 进入内核空间（trampoline.S: uservec）**

- **关键操作**：  
    硬件跳转到`stvec`指向的`uservec`，保存用户寄存器并切换到内核上下文。
    
    - **寄存器变化**：
        
        - `sscratch`：与`a0`交换，指向进程的`TRAPFRAME`地址。
            
        - 所有用户寄存器（`ra`, `sp`, `a1`-`a7`等）被保存到`TRAPFRAME`中。
            
        - `sp`：切换为内核栈指针（`p->kstack + PGSIZE`）。
            
        - `satp`：切换为内核页表。
            
        - `t0`：跳转到`usertrap()`函数地址。
            

---

### **3. 内核处理系统调用（trap.c: usertrap）**

- **关键操作**：  
    验证中断来源后处理系统调用，调用`syscall()`执行具体逻辑。
    
    - **寄存器变化**：
        
        - `p->trapframe->epc`：`ecall`地址被修改为`epc += 4`（跳过`ecall`指令）。
            
        - `stvec`：被设置为`kernelvec`（后续内核中断由此处理）。
            
        - `a0`：保存系统调用返回值（由`syscall()`填充）。
            

---

### **4. 准备返回用户空间（trap.c: usertrapret）**

- **关键操作**：  
    关闭中断，设置返回用户空间的上下文。
    
    - **寄存器变化**：
        
        - `stvec`：重置为`TRAMPOLINE + uservec`（用户中断入口）。
            
        - `sstatus`：
            
            - `SPP`位清零（标记返回用户模式）。
                
            - `SPIE`位置1（允许用户中断）。
                
        - `sepc`：设置为`p->trapframe->epc`（用户程序下一条指令地址）。
            
        - `satp`：指向用户页表（通过`MAKE_SATP(p->pagetable)`）。
            

---

### **5. 返回用户空间（trampoline.S: userret）**

- **关键操作**：  
    恢复用户寄存器，切换页表，执行`sret`返回用户程序。
    
    - **寄存器变化**：
        
        - `satp`：切换为用户页表。
            
        - `sscratch`：恢复用户`a0`的值。
            
        - 所有用户寄存器（`ra`, `sp`, `a0`-`a7`等）从`TRAPFRAME`恢复。
            
        - `sret`：
            
            - `pc` = `sepc`（指向用户程序的下一条指令）。
                
            - `sstatus.SPP` = 0（回到U模式）。
                

---

### **关键寄存器总结**

|阶段|关键寄存器状态|
|---|---|
|**用户触发ecall**|`sepc=ecall地址`, `scause=8`, `sstatus.SPP=0`, `stvec=uservec`|
|**uservec保存上下文**|`sscratch=TRAPFRAME`, `a0=TRAPFRAME地址`, `sp=内核栈`, `satp=内核页表`|
|**usertrap处理**|`p->trapframe->epc+=4`, `stvec=kernelvec`, `a0=系统调用返回值`|
|**usertrapret设置**|`stvec=uservec`, `sepc=用户下一条指令`, `sstatus.SPP=0且SPIE=1`|
|**userret恢复**|`satp=用户页表`, `sscratch=用户a0`, `sret跳转至sepc地址`|