现代操作系统通常使用虚拟内存管理，把内存分成内核空间和用户空间。内核空间是操作系统内核运行的地方，用户空间则是应用程序运行的地方。
## 内核虚拟地址物理地址映射
![[Pasted image 20250504113854.png]]
## 用户地址空间
![[Pasted image 20250504113941.png]]


### **内核空间（Kernel Space）**

1. **定义**
    
    - 操作系统内核运行的特权区域，直接管理硬件资源（CPU、内存、设备等）。
        
    - 拥有最高权限（**内核态**），可执行所有CPU指令（如操作硬件、修改内存映射）。
        
2. **功能**
    
    - 处理系统调用（System Calls）：用户程序通过系统调用请求内核服务（如文件读写、网络通信）。
        
    - 管理进程调度、内存分配、设备驱动等核心任务。
        
    - 处理中断和异常（如硬件中断、缺页错误）。
        
3. **内存访问**
    
    - 内核空间对所有进程共享，但受保护，用户程序无法直接访问。
        
4. **稳定性要求**
    
    - 内核代码必须高度可靠，错误可能导致系统崩溃（如内核恐慌或蓝屏）。
        

---

### **用户空间（User Space）**

1. **定义**
    
    - 普通应用程序运行的非特权区域，每个进程拥有独立的虚拟地址空间。
        
    - 权限受限（**用户态**），无法直接访问硬件或内核内存。
        
2. **功能**
    
    - 运行用户级程序（如浏览器、文本编辑器）。
        
    - 通过系统调用委托内核完成特权操作（如打开文件、创建进程）。
        
3. **内存访问**
    
    - 进程间内存隔离：一个进程崩溃通常不会影响其他进程或系统。
        
    - 通过虚拟内存机制映射到物理内存，由内核管理。
        
4. **安全性**
    
    - 用户程序权限受限，防止恶意操作破坏系统或其他进程。
        

---

### **关键区别**

| **特性**   | **内核空间**     | **用户空间**    |
| -------- | ------------ | ----------- |
| **权限**   | 内核态（特权模式）    | 用户态（非特权模式）  |
| **内存访问** | 直接访问所有硬件和内存  | 只能访问自己的虚拟内存 |
| **代码运行** | 操作系统内核       | 用户应用程序      |
| **崩溃影响** | 导致系统崩溃       | 仅影响当前进程     |
| **通信方式** | 通过系统调用、中断、异常 | 依赖内核提供的接口   |