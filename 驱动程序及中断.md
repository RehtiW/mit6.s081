### **1. UART 硬件的工作机制**

#### **(1) 发送缓冲区（FIFO）**

- UART 内部包含一个 **硬件 FIFO 缓冲区**（例如 16 字节），用于临时存储待发送的字符。
    
- CPU 只需将字符写入 UART 的数据寄存器，硬件会自动将字符存入 FIFO，并开始 **异步串行传输**（无需 CPU 干预）。
    

#### **(2) 发送过程**

1. **写入数据**：驱动程序（如 `uartputc()`）将字符 `$` 写入 UART 的数据寄存器（例如 `THR`，Transmit Holding Register）。
    
2. **硬件接管**：UART 将字符从数据寄存器移入发送 FIFO。
    
3. **串行化传输**：UART 硬件将 FIFO 中的字符按位（bit）转换为电信号，通过串行线路发送到控制台（如物理终端或虚拟控制台）。
    
4. **传输完成**：当 FIFO 为空时，UART 可能触发中断通知 CPU（或通过状态寄存器标记可继续写入）。